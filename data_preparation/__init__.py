"""Data-preparation utilities for the pipeline.

Two main entry points:
  * ``unzip_kseq.py`` — explode the AICUP KSeq archives into the raw layout.
  * ``prepare_bst_dataset.py`` — re-run the BST training-data pipeline
    (legacy ``preparing_data/prepare_train_on_shuttleset.py`` wrapped).
"""
